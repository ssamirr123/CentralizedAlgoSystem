# Backend (FastAPI on EC2, behind Nginx)
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
sqlalchemy>=2.0.32
alembic>=1.13,<2.0
httpx>=0.27.0
psycopg2-binary>=2.9.0
boto3>=1.34.0

# Auth / security
bcrypt>=4.1.0
PyJWT>=2.9.0

# Used by the trading strategies (dataframes) and the strategy-side
# heartbeat / log-shipper / reporting agents (sync HTTP).
pandas>=2.2.2
requests>=2.32.0

# Market-data engine (Stage 19). ICICI Breeze SDK. Imported lazily by
# trading/market_data/providers/icici_breeze.py so the package still
# imports (and the unit suite still runs) if this is absent.
breeze-connect>=1.0.0
